"use client";

type Props = {
  value: string;
  onChange: (value: string) => void;
};

export function MaskedInput({ value, onChange }: Props) {
  return (
    <div>
      <p>teste do input</p>
    </div>
  );
}
